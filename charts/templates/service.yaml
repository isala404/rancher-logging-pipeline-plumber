apiVersion: v1
kind: Service
metadata:
  name: {{ include "rancher-logging-explorer.fullname" . }}
  labels:
    {{- include "rancher-logging-explorer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rancher-logging-explorer.selectorLabels" . | nindent 4 }}
